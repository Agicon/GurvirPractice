"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var getCircularReplacer = function () {
    var seen = new WeakSet();
    return function (key, value) {
        if (typeof value === 'object' && value !== null) {
            if (seen.has(value)) {
                return;
            }
            seen.add(value);
        }
        return value;
    };
};
var hidePrivateKeys = function (source) {
    var replacer = getCircularReplacer();
    var hide = function (source) {
        return Object.entries(source).reduce(function (acc, _a) {
            var key = _a[0], value = _a[1];
            if (/^_/.test(key)) {
                acc._ = (acc._ || {});
                acc._[key.replace(/^_+/, '')] = value;
            }
            else {
                var uniqValue = replacer(key, value);
                acc[key] = !!uniqValue && typeof uniqValue === 'object' ? hide(uniqValue) : uniqValue;
            }
            return acc;
        }, {});
    };
    return hide(source);
};
exports.default = (function (source) {
    return JSON.stringify(hidePrivateKeys(source), getCircularReplacer());
});
//# sourceMappingURL=convertJSON.js.map